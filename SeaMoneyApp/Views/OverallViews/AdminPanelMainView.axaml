<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           
             
             xmlns:overalViewModels="clr-namespace:SeaMoneyApp.ViewModels.OveralViewModels"
             d:DataContext="{d:DesignInstance overalViewModels:AdminPanelMainViewModel, IsDesignTimeCreatable=True}"
             x:DataType="overalViewModels:AdminPanelMainViewModel"
             
             x:Class="SeaMoneyApp.Views.OverallViews.AdminPanelMainView">
        <StackPanel Orientation="Vertical">
            <ComboBox  x:Name="ComboBoxSelector" ItemsSource="{Binding Data.Keys}"
                      SelectedItem="{Binding SelectedList}"/>
            <!-- <ListBox ItemsSource="{Binding Accounts}"/> -->
            <DataGrid ItemsSource="{Binding SelectedData}"
                      GridLinesVisibility="All"
                      Margin="20"
                      AutoGenerateColumns="True">
                <!-- <DataGrid.Columns> -->
                <!--     <DataGridTextColumn Header="First Name"  Binding="{Binding Login}"/> -->
                <!--     <DataGridTextColumn Header="Last Name" Binding="{Binding Password}" /> -->
                <!-- </DataGrid.Columns> -->
            </DataGrid>
        </StackPanel>
</UserControl>
