<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           
             
             xmlns:overalViewModels="clr-namespace:SeaMoneyApp.ViewModels.OverallViewModels"
             x:DataType="overalViewModels:AdminPanelMainViewModel"
             
             x:Class="SeaMoneyApp.Views.OverallViews.AdminPanelMainView">
        <StackPanel Orientation="Vertical">
            <ComboBox  x:Name="ComboBoxSelector" ItemsSource="{Binding Data.Keys}"
                      SelectedItem="{Binding SelectedList}"/>
            <DataGrid ItemsSource="{Binding SelectedData}"
                      GridLinesVisibility="All"
                      Margin="20"
                      AutoGenerateColumns="True">
            </DataGrid>
        </StackPanel>
</UserControl>
